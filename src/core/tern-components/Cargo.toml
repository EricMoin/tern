[package]
name = "tern-components"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "tern components: the compositor and imperative Text/Box renderables"

[dependencies]
tern-core = { path = "../tern-core" }
tern-layout = { path = "../tern-layout" }

# Bench/test-only: the integration-test benches flush diffs through
# tern-terminal's write-based `flush_diff_to` (see tests/bench_timing.rs).
[dev-dependencies]
tern-terminal = { path = "../tern-terminal" }
