{
  "name": "@tern/solid",
  "version": "0.1.0",
  "private": true,
  "description": "tern solid — SolidJS custom renderer for tern",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "check": "deno check src/index.ts",
    "check:tsc": "tsc --noEmit",
    "test": "deno test src",
    "build": "deno task build",
    "build:tsc": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@tern/core": "^0.1.0",
    "solid-js": "^1.9.14"
  }
}
